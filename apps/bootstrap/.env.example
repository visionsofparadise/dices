# Concurrency of find operations on the overlay network
CONCURRENCY=3

# IPv4 or IPv6 address to bind to
IPV4=127.0.0.1
# IPV6=::1

# Log level (0-5, 0 being all logs, 5 being silent)
LOG_LEVEL=1

# Comma separated list of ports
PORTS=5173,5174,5175

# Comma separated list of secret keys as hex (64 hex chars each = 32 bytes)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SECRET_KEYS=
